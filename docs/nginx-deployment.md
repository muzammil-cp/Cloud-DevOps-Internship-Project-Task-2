# Nginx Deployment

## Objective

Install and configure Nginx as the web server on the Ubuntu EC2 instance.

## Requirements

- Running Ubuntu EC2 instance
- SSH access
- Internet connectivity
- Nginx package

## Configuration

Nginx was installed on the Ubuntu EC2 instance.

The service was configured to start automatically.

## Commands

Update packages:

```bash
sudo apt update
